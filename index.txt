<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Atlas Multiplayer Game</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg-main: #121212;
      --bg-card: #1e1e1e;
      --text-primary: #e0e0e0;
      --text-secondary: #b0b0b0;
      --accent: #6c5ce7;
      --accent-hover: #5f4bb8;
      --success: #00b894;
      --danger: #d63031;
      --border: #333;
      --input-bg: #2d2d2d;
      --input-border: #444;
      --shadow: 0 4px 12px rgba(0,0,0,.3);
      --radius: 12px;
      --transition: all .3s ease;
    }
    html, body { margin:0; padding:0; height:100%; }
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #121212, #0a0a0a);
      color: #e0e0e0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    header {
      background: var(--bg-card);
      padding: 16px 20px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0,0,0,.2);
      border-bottom: 1px solid var(--border);
    }
    header h1 {
      margin:0;
      font-size:1.8rem;
      background: linear-gradient(90deg,var(--accent),#a29bfe);
      -webkit-background-clip:text;
      -webkit-text-fill-color:transparent;
      font-weight:700;
    }
    /* LOBBY */
    #lobby {
      display:flex; flex-direction:column; gap:16px;
      padding:28px; background:var(--bg-card);
      margin:24px auto; border-radius:var(--radius);
      box-shadow:var(--shadow); max-width:480px; width:100%;
      border:1px solid var(--border);
    }
    input, select {
      padding:14px 16px; background:var(--input-bg);
      border:1px solid var(--input-border); border-radius:var(--radius);
      color:var(--text-primary); font-size:1rem;
      transition:var(--transition);
    }
    input:focus, select:focus {
      outline:none; border-color:var(--accent);
      box-shadow:0 0 0 2px rgba(108,92,231,.3);
    }
    input::placeholder { color:#777; }
    button {
      padding:14px 18px; border:none; border-radius:var(--radius);
      cursor:pointer; font-weight:600; font-size:1rem;
      transition:var(--transition);
    }
    #createRoomBtn, #joinRoomBtn {
      background:var(--accent); color:#fff;
    }
    #createRoomBtn:hover, #joinRoomBtn:hover {
      background:var(--accent-hover); transform:translateY(-2px);
    }

    /* GAME GRID */
    #game { display:none; flex:1; }
    .game-grid {
      display:grid;
      grid-template-columns:1fr 2fr 1fr;
      height:100%;
      gap:0;
      min-height: 70vh;
    }

    .card {
      background:var(--bg-card); padding:20px;
      overflow:hidden; display:flex; flex-direction:column;
      border:1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      height: 100%;
    }

    /* LEFT – Room Code */
    .room-code-panel {
      justify-content:center; align-items:center; display: flex;
    }
    #roomCodeOnly {
      font-size: 1.4rem;
      color: var(--accent);
      text-align: center;
      font-weight: 600;
      margin: auto;
      border-bottom: 1px solid var(--border);
      padding-bottom: 10px;
    }

    /* CENTER – Input & Message */
    .centre-panel {
      justify-content:center; align-items:center; gap:22px; display:flex; flex-direction:column;
    }
    #countryInput {
      width:100%; padding:16px 18px; font-size:1.1rem;
      min-height:58px;
      margin-bottom:12px;
      background: var(--input-bg);
    }
    #waitingMessage {
      width:100%; padding:14px 18px;
      background:rgba(108,92,231,.15); border-radius:var(--radius);
      font-size:1.05rem; color:var(--accent); text-align:center;
      min-height:54px; display:flex; align-items:center;
      justify-content:center; border:1px solid rgba(108,92,231,.3);
      margin-bottom:16px;
    }
    .centre-panel .buttons {
      display:flex; gap:12px; width:100%; margin-bottom:16px;
    }
    .centre-panel .buttons button {
      flex:1; padding:14px; font-size:1rem;
      background:var(--accent); color:#fff;
    }
    .centre-panel .buttons button:hover { background:var(--accent-hover); }

    /* RIGHT – History */
    .history-panel {
      flex: 1; overflow-y: auto;
    }
    .history-panel h3 {
      margin:0 0 14px; color:var(--accent);
      font-size:1.3rem; border-bottom:1px solid var(--border);
      padding-bottom:8px;
    }
    #history {
      list-style:none; padding:0; margin:0;
      overflow-y:auto; font-size:.9rem;
    }
    #history li {
      padding:8px 0; border-bottom:1px solid #2a2a2a;
      color:var(--text-secondary);
    }

    /* Responsive */
    @media (max-width:992px) {
      .game-grid { grid-template-columns:1fr; }
      .room-code-panel, .history-panel { max-height:200px; overflow-y:auto; }
      .centre-panel { padding:20px; }
    }
    @media (max-width:600px) {
      #lobby { padding:20px; margin:16px; }
      header h1 { font-size:1.5rem; }
      .centre-panel .buttons { flex-direction:column; }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>Atlas Multiplayer Game</h1>
    </header>

    <!-- LOBBY (unchanged) -->
    <div id="lobby">
      <input id="playerName" placeholder="Enter Your Name" required>
      <select id="roundsSelect">
        <option value="1">1 Round</option><option value="2">2 Rounds</option>
        <!-- ... -->
        <option value="15">15 Rounds</option>
      </select>
      <button id="createRoomBtn" onclick="createRoom()">Create Room</button>
      <input id="roomIdInput" placeholder="Room ID (if joining)">
      <button id="joinRoomBtn" onclick="joinRoom()">Join Room</button>
    </div>

    <!-- GAME GRID -->
    <div id="game">
      <div class="game-grid">

        <!-- LEFT – Room Code Only -->
        <section class="room-code-panel card">
          <div id="roomCodeOnly">Room: <span id="roomCodeDisplay">XXXXXX</span></div>
        </section>

        <!-- CENTER – Input and Message -->
        <section class="centre-panel card">
          <input id="countryInput" placeholder="Enter Place Name">
          <div class="buttons">
            <button onclick="submitCountry()">Submit</button>
            <button onclick="giveUp()" class="giveup">Give Up</button>
          </div>
          <div id="waitingMessage">Waiting for [person] to answer...</div>
        </section>

        <!-- RIGHT – History -->
        <section class="history-panel card">
          <h3>History</h3>
          <ul id="history"></ul>
        </section>
      </div>
    </div>
    <!-- WINNER MODAL and IMAGE LOADING MODAL remain unchanged -->

    <script src="/socket.io/socket.io.js"></script>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        document.getElementById('winnerModal').style.display = 'none';
        document.getElementById('imageModal').style.display = 'none';
      });
      // Add JS here to update #roomCodeDisplay, #waitingMessage, #history as needed from game state
    </script>
    <script src="client.js"></script>
  </div>
</body>
</html>
